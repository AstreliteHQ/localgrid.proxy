# @astrelitehq/localgrid is published to GitHub Packages, not the public npm
# registry. NODE_AUTH_TOKEN needs read:packages access: GITHUB_TOKEN in CI
# (pages.yml), or a personal access token exported locally.
@astrelitehq:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
